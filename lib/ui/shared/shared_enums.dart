enum LoginProfileType {
  google,
  facebook,
}

enum ReportHeaderTypes {
  AreaId,
  AreaName,
  AreaRegisterDate,
  SensorNumber,
}

enum ReportAdditionalHeaderTypes {
  AmountAreas,
  AmountSensors,
}

enum OrderBy {
  alphabetical,
  highestValue,
  lowerValue,
  mostRecent,
  leastRecent,
}

enum NotificationType {
  weather,
  monitoring,
}